{
  "name": "app",
  "version": "1.0.0",
  "description": "Serverless app to route etherscan API responses. Uses load balancing to request",
  "main": "handler.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "aws-serverless-express": "^3.3.6",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "helmet": "^3.15.0",
    "validator": "^10.11.0",
    "web3-utils": "^1.0.0-beta.52"
  },
  "devDependencies": {
    "cross-env": "^5.2.0"
  },
  "scripts": {
    "deploy": "cross-env AWS_PROFILE=torus serverless deploy -v",
    "debug": "cross-env AWS_PROFILE=torus serverless invoke local -f app -l",
    "update": "cross-env AWS_PROFILE=torus serverless deploy",
    "update:app": "cross-env AWS_PROFILE=torus serverless deploy function -f app",
    "prod": "cross-env AWS_PROFILE=torus serverless deploy -v",
    "remove": "cross-env AWS_PROFILE=torus serverless remove"
  }
}
